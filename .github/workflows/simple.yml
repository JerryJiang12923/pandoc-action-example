name: Simple Usage

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-22.04
    steps:
      - uses: docker://pandoc/latex:2.9
        with:
          args: "-f docx -t markdown foo.docx -o foo.markdown" # gets appended to pandoc command
