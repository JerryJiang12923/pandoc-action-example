name: Simple Usage

on: workflow_dispatch

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-22.04
    steps:
      - uses: docker://pandoc/latex:2.9
        with:
          args: "-f docx -t markdown 黄山徒步路线体力消耗优化模型研究_蒋宇皓.docx -o foo.markdown" # gets appended to pandoc command
